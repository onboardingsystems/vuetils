import ObsCompoundLayout from './obs-compound-layout';

ObsCompoundLayout.install = function install(Vue) {
  Vue.component(ObsCompoundLayout.name, ObsCompoundLayout)
};

export default ObsCompoundLayout;
